# 本地运行说明（本机部署记录）

本目录是把 GitHub 仓库 `<GitHub账号>/xwlb-report` 克隆到本机后的可运行副本，并已完成**模块化重构**。

- 本机路径：`<本机仓库路径>`
- 克隆时间：2026-09-15　提交：`a2fba32`（origin/main）
- 重构记录：见 `docs\REFACTOR-重构实施记录.md`；设计依据见 `docs\SDD-软件设计说明书.md`

| 时间 | 变更 |
| --- | --- |
| 2026-09-15 | 初次克隆并跑通 |
| 2026-09-15 | **完全重构**：单文件脚本 → `scripts\xwlb\` 模块包（14 模块）+ 2 个兼容薄壳；修复 13 项缺陷；离线验证套件扩至 121 项 |

## 1. 环境要求

| 项目 | 状态 |
| --- | --- |
| Python | 3.11.8（纯标准库，**无需 pip install**，无 `requirements.txt`） |
| 网络 | 需可访问 `api.cntv.cn`、`tv.cctv.com`、`www.bing.com`、`html.duckduckgo.com`、`api.deepseek.com` |
| DeepSeek Key | 已就绪：`%USERPROFILE%\.dsh\.credentials.yaml` 中的 `DEEPSEEK_API_KEY` |
| SMTP 凭据 | **未配置**（见第 3 节）；未配置时按设计跳过发送，不影响日报生成 |

## 2. 运行命令

在 `<本机仓库路径>` 目录下执行（PowerShell）：

```powershell
# ① 仅采集，验证网络与央视通道（不调用 API、不写文件、不发信）
python scripts\standalone_report.py --fetch-only --date 2026-09-15

# ② 完整流程，生成日报但不发邮件（推荐先跑这个；约 6–7 分钟）
python scripts\standalone_report.py --date 2026-09-15 --dry-run

# ③ 完整流程并真实发信（需要第 3 节的 SMTP 凭据）
python scripts\standalone_report.py --date 2026-09-15

# ④ 单独补发某日已生成的日报（marker 去重）
python scripts\send_report.py --date 2026-09-15 --dry-run

# ⑤ 离线自检（不触网，秒级；重构后的主要回归手段）
python docs\verification\unit_checks.py      # 121 项检查，应全部 PASS
python docs\verification\code_metrics.py     # 代码度量与调用图
```

常用开关：

| 开关 | 作用 |
| --- | --- |
| `--date YYYY-MM-DD` | 指定日期（缺省今天，北京时间） |
| `--dry-run` | 只生成不发送 |
| `--force` | 忽略 `reports\.sent-<日期>.marker`，强制重跑/重发 |
| `--fetch-only` | 仅采集，不调 API / 不写文件 / 不发信 |
| `--skip-retrieval` | 跳过形势检索与市场检索（明显更快） |
| `--skip-extra` | 跳过增强板块（省一次 LLM 调用） |
| `--skip-verify` | 跳过来源链接核验（提速） |
| `--no-alert` | 失败时不发告警邮件 |
| `--api-key` / `--model` | 直接指定 Key / 首选模型 |
| `--config` / `--out` | 指定 SMTP 配置 / 日报输出路径 |

> 注意：`reports\` 下已有 `.sent-2026-09-03/05/06/12.marker`（已入库去重标记）。若目标日期 marker 已存在，脚本打印
> `[跳过] 当日日报已发送` 并退出 0；要重跑请加 `--force`。

## 3. 发信配置（唯一需要你补的一步）

凭据解析顺序：`SMTP_*` 环境变量 > `config\smtp_config.json` > 默认值（`smtp.qq.com:465/SSL/30s`）。

1. 复制 `config\smtp_config.example.json` 为 `config\smtp_config.json`（本机已有一份**空模板**）；
2. 填入 `sender`（发信 QQ 邮箱）、`authcode`（QQ 邮箱 SMTP **授权码**，非登录密码）、`recipient`（收件人数组）。

**凭据缺失时的行为（重构后已统一，DD-10）**

| 入口 | 行为 | 退出码 |
| --- | --- | --- |
| `standalone_report.py`（主管线） | 日报**照常生成并落盘**，仅跳过发送，并打印 `::warning::` 与告警尝试 | 0 |
| `send_report.py`（单独推送） | 配置缺失即报错退出（保持原语义） | 3 |

也可改用环境变量（云端 GitHub Actions 走这条）：

```powershell
$env:SMTP_SENDER="xxx@qq.com"; $env:SMTP_AUTHCODE="授权码"; $env:SMTP_RECIPIENT="a@b.com,c@d.com"
```

## 4. 目录结构（重构后）

```
xwlb-report\
├── scripts\
│   ├── xwlb\                     ← 领域包（14 个模块）
│   │   ├── common.py             M-01  路径/北京时间/日志/裁剪/字节计量
│   │   ├── net.py                M-01  网络接缝（Fetcher）/ 解码 / HTML 净化
│   │   ├── search.py             M-02~05  搜索适配 / 白名单 / 查询 / 检索配额
│   │   ├── acquire.py            M-06~07  央视采集（5 阶段）/ 条目抽取
│   │   ├── llm.py                M-08  Key 解析 / DeepSeek / 模型降级链
│   │   ├── prompts.py            M-08~09  提示词 / 消息组装 / 注入防护
│   │   ├── render.py             M-10  去分净化 / 板块装配 / 缺失说明
│   │   ├── state.py              M-11  v3 schema / 键归一化 / 原子写 / 迁移
│   │   ├── verify.py             M-12  来源链接核验
│   │   ├── deliver.py            M-12  SMTP 配置 / Markdown→HTML / 投递
│   │   ├── alert.py              M-14  失败告警
│   │   ├── metrics.py            —     阶段耗时计量
│   │   └── cli.py                M-13  CLI 与编排
│   ├── standalone_report.py      ← 兼容薄壳（CLI + 历史符号再导出）
│   └── send_report.py            ← 兼容薄壳
├── docs\
│   ├── Spec-需求规格说明书.md     需求基线（18 条功能需求）
│   ├── SDD-软件设计说明书.md      设计基线（14 模块 / 11 算法 / 15 项修复设计）
│   ├── REFACTOR-重构实施记录.md   本次重构的落实表与前后的度量对比
│   └── verification\
│       ├── unit_checks.py        离线验证套件（121 项，不触网）
│       └── code_metrics.py       AST 度量与调用图
├── config\smtp_config.json       本机 SMTP 凭据（gitignore；当前为空模板）
├── config\smtp_config.example.json 模板与字段说明
├── reports\YYYY-MM-DD.md         生成的日报（gitignore）
├── reports\state.json            跨日状态 v3（gitignore）
├── reports\.sent-*.marker        已发送去重标记（**入库**）
├── logs\standalone.log           运行日志（gitignore）
├── logs\verify-refactor-dryrun.txt 重构后端到端验证日志（gitignore）
└── .github\workflows\daily_report.yml 云端定时任务 + 离线验证作业
```

## 5. 本机与云端的关系

仓库设计为**云端单路径**：GitHub Actions 每天北京 21:00/21:30/22:00/22:30/23:00 五班重试，
凭据走 GitHub Secrets，发送成功后把 marker 提交回仓库实现当天去重。

因此**在本机跑完整流程会与云端抢发**（同一天先成功者写 marker，后到者自动跳过）。
本机建议只用于补跑、调试，或使用 `--dry-run`。

## 6. 本机验证记录

**重构后（2026-09-15）**

| 验证 | 结果 |
| --- | --- |
| `python -m py_compile`（16 个文件） | 通过 |
| `docs\verification\unit_checks.py` | **121 项检查，121 PASS / 0 FAIL** |
| `--fetch-only --date 2026-09-15` | 央视 API 命中，brief 742 字符，**24 条**（与官方口径一致） |
| `--date 2026-09-15 --dry-run` | **退出码 0**，合计 403.2 秒；主报告 11,569 字符；日志见 `logs\verify-refactor-dryrun.txt` |
| 产物行尾 | `reports\2026-09-15.md`、`state.json` 均 **CRLF 0 组**（纯 LF） |
| 代码度量 | 最长模块 406 行、最长函数 45 行、最大分支点 10（重构前为 1050 / 118 / 21） |

> 重构前的验证日志（`logs\verify-dryrun.txt`）已随本次清理删除；其关键数字在
> `docs\Spec-需求规格说明书.md` 附录 A 中有逐字摘录。

## 7. 排障

| 现象 | 原因 | 处置 |
| --- | --- | --- |
| git 报 `schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS` | 本机 git 默认 TLS 后端不可用 | 已对本仓库设 `git config http.sslBackend openssl`；其他目录用 `git -c http.sslBackend=openssl clone <url>` |
| `[失败] 未找到 DEEPSEEK_API_KEY` | 三级来源均无 | 检查 `--api-key` / 环境变量 / `~/.dsh/.credentials.yaml` |
| `[失败] 未找到发信配置`（退出 3） | 仅 `send_report.py` 会如此 | 按第 3 节补齐配置，或改用 `standalone_report.py` |
| `[跳过] 配置缺失`，日报已生成 | 主管线的设计行为（DD-10） | 正常；补齐凭据即可发送 |
| `[跳过] 当日日报已发送` | marker 存在 | 加 `--force` |
| 当日无数据、提示节目 19:00 才播出 | 央视 API 当日条目滞后 2–4 小时 | 正常空窗，稍后重跑 |
| 日报总条数异常（>25 或出现非编号条目） | 条目抽取质量 | 看日志 `[采集] 抽取统计：… 疑似小数=N`；按 `acquire._ITEM_PATTERN` 排查 |
| `[状态] 迁移 v2 → v3` | 首次运行新版会迁移旧状态 | 正常；迁移前会自动备份 |
| 出现 `[警告] 锚点缺失，板块被丢弃` | LLM 未按模板输出某个增强板块 | 属降级，日报仍可用；观察是否复发（DD-14） |
| 出现 `[警告] 阶段=主报告 秒=…` | 单阶段超过 120 秒 | 属提示，非错误（DD-08） |

## 8. 清理与回滚

**清理构建产物**（Python 字节码缓存，可随时重建）

```powershell
Get-ChildItem -Recurse -Force -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force
```

> 不想每次生成缓存，可临时设置 `$env:PYTHONDONTWRITEBYTECODE=1` 再运行。

**回滚重构**

```powershell
# 回到重构前的脚本与工作流（重构前代码完整保存在 commit a2fba32）
git checkout HEAD -- scripts\ .github\
Remove-Item -Recurse -Force scripts\xwlb      # 移除新包
```

> 注意：`reports\`、`logs\`、`config\`、`docs\` 属未跟踪/被忽略内容，`git checkout` 不会触碰它们。
> 另：迁移备份 `reports\state.v2.bak` 已于本次清理中删除；如需复现 v2→v3 迁移，
> 可用 `docs\verification\unit_checks.py` 中的合成夹具 `V2_FIXTURE`。
